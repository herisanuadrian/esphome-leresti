# DEPENDS ON: (nothing)
# EXPOSES: (nothing — platform configuration)

wifi:
  ssid: !secret wifi_ssid_nou
  password: !secret wifi_password_nou
  fast_connect: true
  power_save_mode: none
  ap:
    ssid: "your-device-name-fallback"
    password: !secret ap_password

captive_portal:
