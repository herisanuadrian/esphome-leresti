# Logging — Overview

Logger verbosity configuration.

## Files

**logger.yaml** — global level DEBUG, with a per-tag override for `xpt2046` (touch driver) also at DEBUG, matching the touch debug lambda in `hardware/touchscreen.yaml`.

## Exposes

(nothing — internal logging configuration only)

## Dependencies

None.
