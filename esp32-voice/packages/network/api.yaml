# DEPENDS ON: api_encryption_key_esp32_voice (secret)
# EXPOSES: (nothing named — device registers as an HA Assist voice satellite via api: + voice_assistant:)

api:
  encryption:
    key: !secret api_encryption_key_esp32_voice
