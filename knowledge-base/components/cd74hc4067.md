# ESPHome component: `cd74hc4067`

Source: `esphome/components/cd74hc4067/ at ref 2026.7.2`

> Auto-extracted public C++ interface. Not exhaustive — for anything
> not covered here, check the `.py` config schema in the same folder
> or the source file directly before guessing a lambda call.

## `cd74hc4067.h`

**class `CD74HC4067Component` — public interface:**
```cpp
void setup() override;
void dump_config() override;
void activate_pin(uint8_t pin);
void set_pin_s0(GPIOPin *pin) { this->pin_s0_ = pin; }
void set_pin_s1(GPIOPin *pin) { this->pin_s1_ = pin; }
void set_pin_s2(GPIOPin *pin) { this->pin_s2_ = pin; }
void set_pin_s3(GPIOPin *pin) { this->pin_s3_ = pin; }
void set_switch_delay(uint32_t switch_delay) { this->switch_delay_ = switch_delay; }
```

**class `CD74HC4067Sensor` — public interface:**
```cpp
CD74HC4067Sensor(CD74HC4067Component *parent);
void update() override;
void dump_config() override;
float get_setup_priority() const override;
void set_pin(uint8_t pin) { this->pin_ = pin; }
void set_source(voltage_sampler::VoltageSampler *source) { this->source_ = source; }
float sample() override;
```
