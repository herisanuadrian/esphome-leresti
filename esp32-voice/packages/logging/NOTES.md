# Logging — Overview

## Files

**logger.yaml** — `logger: hardware_uart: UART0, level: INFO`, no per-component
overrides. Carried over as-is from the known-working flat config this was split
from.

## Exposes

None.

## Dependencies

None.
